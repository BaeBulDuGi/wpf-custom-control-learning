<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- 애니메이션이 적용된 Control Template -->
    <!-- WPF의 강력한 기능: 코드 없이 XAML만으로 애니메이션 구현 -->

    <!-- Easing Functions (부드러운 애니메이션) - 먼저 정의해야 함 -->
    <CubicEase x:Key="EaseOut" EasingMode="EaseOut"/>
    <CircleEase x:Key="CircleEaseOut" EasingMode="EaseOut"/>
    <BackEase x:Key="BackEaseOut" EasingMode="EaseOut" Amplitude="0.3"/>

    <!-- 1. 부드러운 크기 변화 버튼 -->
    <Style x:Key="AnimatedScaleButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="#FF2196F3"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Padding" Value="20,10"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
        <Setter Property="RenderTransform">
            <Setter.Value>
                <ScaleTransform ScaleX="1" ScaleY="1"/>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            CornerRadius="10">
                        <ContentPresenter HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Margin="{TemplateBinding Padding}"/>
                    </Border>

                    <!-- 애니메이션 Triggers -->
                    <ControlTemplate.Triggers>
                        <!-- Hover: 1.1배 확대 + 색상 변화 -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <!-- 크기 애니메이션 -->
                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                       To="1.1"
                                                       Duration="0:0:0.2"
                                                       EasingFunction="{StaticResource EaseOut}"/>
                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                       To="1.1"
                                                       Duration="0:0:0.2"
                                                       EasingFunction="{StaticResource EaseOut}"/>
                                        <!-- 색상 애니메이션 -->
                                        <ColorAnimation Storyboard.TargetName="border"
                                                      Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                      To="#FF42A5F5"
                                                      Duration="0:0:0.2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                       To="1.0"
                                                       Duration="0:0:0.2"
                                                       EasingFunction="{StaticResource EaseOut}"/>
                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                       To="1.0"
                                                       Duration="0:0:0.2"
                                                       EasingFunction="{StaticResource EaseOut}"/>
                                        <ColorAnimation Storyboard.TargetName="border"
                                                      Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                      To="#FF2196F3"
                                                      Duration="0:0:0.2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>

                        <!-- Pressed: 0.95배 축소 -->
                        <Trigger Property="IsPressed" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                       To="0.95"
                                                       Duration="0:0:0.1"/>
                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                       To="0.95"
                                                       Duration="0:0:0.1"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 2. 로딩 스피너 (회전 애니메이션) -->
    <Style x:Key="LoadingSpinnerStyle" TargetType="Control">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Control">
                    <Grid x:Name="SpinnerGrid" RenderTransformOrigin="0.5,0.5">
                        <Grid.RenderTransform>
                            <RotateTransform x:Name="SpinnerRotate" Angle="0"/>
                        </Grid.RenderTransform>

                        <!-- 8개의 점으로 구성된 스피너 -->
                        <Ellipse Width="8" Height="8" Fill="#FF2196F3" Opacity="1.0"
                                 HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,5,0,0"/>
                        <Ellipse Width="8" Height="8" Fill="#FF2196F3" Opacity="0.875"
                                 HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,10,10,0"/>
                        <Ellipse Width="8" Height="8" Fill="#FF2196F3" Opacity="0.75"
                                 HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <Ellipse Width="8" Height="8" Fill="#FF2196F3" Opacity="0.625"
                                 HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,10,10"/>
                        <Ellipse Width="8" Height="8" Fill="#FF2196F3" Opacity="0.5"
                                 HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,5"/>
                        <Ellipse Width="8" Height="8" Fill="#FF2196F3" Opacity="0.375"
                                 HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="10,0,0,10"/>
                        <Ellipse Width="8" Height="8" Fill="#FF2196F3" Opacity="0.25"
                                 HorizontalAlignment="Left" VerticalAlignment="Center" Margin="5,0,0,0"/>
                        <Ellipse Width="8" Height="8" Fill="#FF2196F3" Opacity="0.125"
                                 HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,10,0,0"/>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsVisible" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <!-- 무한 회전 애니메이션 -->
                                        <DoubleAnimation Storyboard.TargetName="SpinnerRotate"
                                                       Storyboard.TargetProperty="Angle"
                                                       From="0" To="360"
                                                       Duration="0:0:1"
                                                       RepeatBehavior="Forever"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 3. Fade In/Out 애니메이션 -->
    <Style x:Key="FadeInOutPanelStyle" TargetType="Border">
        <Setter Property="Opacity" Value="0"/>
        <Style.Triggers>
            <Trigger Property="Visibility" Value="Visible">
                <Trigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                           From="0" To="1"
                                           Duration="0:0:0.5"
                                           EasingFunction="{StaticResource EaseOut}"/>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.EnterActions>
                <Trigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                           From="1" To="0"
                                           Duration="0:0:0.3"/>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.ExitActions>
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>
