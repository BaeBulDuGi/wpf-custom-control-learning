<UserControl x:Class="WpfApp.Controls.UserControls.MenuListItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="70" d:DesignWidth="300">
    <Border x:Name="MainBorder"
            Background="#FF3C3C3C"
            BorderBrush="#FF555555"
            BorderThickness="0,0,0,1"
            Cursor="Hand"
            Padding="15,10">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="60"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 왼쪽: 동그란 아이콘 영역 -->
            <Grid Grid.Column="0" Width="50" Height="50">
                <!-- 배경 원 -->
                <Ellipse x:Name="IconBackground"
                         Fill="DodgerBlue"
                         Width="50"
                         Height="50"/>

                <!-- 아이콘 이미지 (있으면 표시) -->
                <Ellipse x:Name="IconImage"
                         Width="50"
                         Height="50">
                    <Ellipse.Fill>
                        <ImageBrush x:Name="IconImageBrush" Stretch="UniformToFill"/>
                    </Ellipse.Fill>
                </Ellipse>

                <!-- 기본 아이콘 (이미지가 없을 때) -->
                <TextBlock x:Name="IconText"
                           Text="●"
                           FontSize="24"
                           Foreground="White"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>
            </Grid>

            <!-- 오른쪽: 텍스트 영역 -->
            <StackPanel Grid.Column="1"
                        VerticalAlignment="Center"
                        Margin="10,0,0,0">
                <TextBlock x:Name="TitleText"
                           Text="Menu Item"
                           FontSize="16"
                           FontWeight="SemiBold"
                           Foreground="White"/>
                <TextBlock x:Name="DescriptionText"
                           Text="Description"
                           FontSize="12"
                           Foreground="LightGray"
                           Margin="0,2,0,0"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
